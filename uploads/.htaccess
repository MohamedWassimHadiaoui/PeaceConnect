# Prevent direct access to uploaded files
# Only allow access through PHP scripts

# Deny all access by default
Order Deny,Allow
Deny from all

# Allow access to avatar images
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Allow from all
</FilesMatch>

